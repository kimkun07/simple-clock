[project]
name = "simple-clock"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "PyQt6>=6.6.0",
    "pyinstaller>=6.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
